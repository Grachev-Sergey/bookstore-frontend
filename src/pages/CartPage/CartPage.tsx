import { CartPageContainer } from './CartPage.styles';

const CartPage: React.FC = () => {
  return (
    <CartPageContainer>
      <p>cart page</p>
    </CartPageContainer>
  );
};

export default CartPage;
